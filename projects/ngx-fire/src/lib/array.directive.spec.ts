import { NgxFireArrayDirective } from './array.directive';

describe('NgxFireArrayDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxFireArrayDirective({} as any, {} as any, {} as any);
    expect(directive).toBeTruthy();
  });
});
