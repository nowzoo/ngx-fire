import { NgxFireBaseDirective } from './ngx-fire-base.directive';

describe('NgxFireBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxFireBaseDirective();
    expect(directive).toBeTruthy();
  });
});
