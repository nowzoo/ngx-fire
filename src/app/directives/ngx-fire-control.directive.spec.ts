import { NgxFireControlDirective } from './ngx-fire-control.directive';

describe('NgxFireControlDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxFireControlDirective();
    expect(directive).toBeTruthy();
  });
});
