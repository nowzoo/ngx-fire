import { NgxFireArrayDirective } from './ngx-fire-array.directive';

describe('NgxFireArrayDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxFireArrayDirective();
    expect(directive).toBeTruthy();
  });
});
