import { NgxFireRefDirective } from './ngx-fire-ref.directive';

describe('NgxFireRefDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxFireRefDirective({} as any);
    expect(directive).toBeTruthy();
  });
});
